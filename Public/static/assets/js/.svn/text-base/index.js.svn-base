$(function() {
    $(window).scroll(function(event) {
        /* Act on the event */
        if ($(window).scrollTop() > 1) {
            $(".header").addClass('fixed-top-bg');
        } else {
            $(".header").removeClass('fixed-top-bg');
        }
    });

    // $(".btn").hover(function() {
    //     /* Stuff to do when the mouse enters the element */
    //     $(".header-qr").show();
    // }, function() {
    //     /* Stuff to do when the mouse leaves the element */
    // });
})